# 🚀 দ্রুত শুরু করুন

## ৩টি সহজ ধাপে চালু করুন

### ১. সার্ভার চালু করুন

```bash
python manage.py runserver
```

### ২. ব্রাউজারে খুলুন

- 🌐 ওয়েব অ্যাপ: http://127.0.0.1:8000/
- 🔌 REST API: http://127.0.0.1:8000/todos/api/todos/

### ৩. Todo তৈরি করুন

- "+ Add Todo" বাটনে ক্লিক করুন
- টাইটেল লিখুন
- "Create Todo" চাপুন

---

## 📱 ওয়েব ইন্টারফেস ব্যবহার

এখানে যান: **http://127.0.0.1:8000/**

আপনি দেখবেন:

- সব Todo এর তালিকা
- ✓ = সম্পন্ন চিহ্নিত করুন
- ✎ = এডিট করুন
- 🗑 = মুছে ফেলুন

---

## 🔌 REST API ব্যবহার

ব্রাউজারে খুলুন: **http://127.0.0.1:8000/todos/api/todos/**

এখানে আপনি:

- সব Todo দেখতে পারবেন
- ফর্ম দিয়ে নতুন Todo তৈরি করতে পারবেন
- যেকোনো Todo ক্লিক করে দেখতে/এডিট/মুছতে পারবেন

### PowerShell দিয়ে টেস্ট

```powershell
# Todo তৈরি
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/" -Method Post -Headers @{"Content-Type"="application/json"} -Body '{"title":"Django শিখুন"}'

# সব Todo দেখা
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/"

# সম্পন্ন চিহ্নিত করা
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/1/" -Method Patch -Headers @{"Content-Type"="application/json"} -Body '{"completed":true}'
```

---

## 🛠️ প্রয়োজনীয় কমান্ড

### এডমিন ইউজার তৈরি

```bash
python manage.py createsuperuser
```

তারপর যান: http://127.0.0.1:8000/admin/

### সার্ভার বন্ধ করা

Terminal এ `Ctrl+C` চাপুন

### সার্ভার আবার চালু করা

```bash
python manage.py runserver
```

---

## 🎯 কি কি করতে পারবেন

**ওয়েব ইন্টারফেস:**

- ✅ Todo তৈরি, দেখা, এডিট, মুছা
- ✅ এক ক্লিকে সম্পন্ন/অসম্পন্ন
- ✅ সুন্দর ডিজাইন
- ✅ মোবাইল ফ্রেন্ডলি

**REST API:**

- ✅ GET, POST, PUT, PATCH, DELETE
- ✅ JSON ডাটা
- ✅ ব্রাউজেবল ইন্টারফেস
- ✅ পেজিনেশন

---

## 📚 ডকুমেন্টেশন

- **README.md** - ইংরেজি গাইড (সম্পূর্ণ)
- **README_BANGLA.md** - বাংলা গাইড (বিস্তারিত)

---

## 📊 API এন্ডপয়েন্ট সংক্ষেপ

| মেথড   | URL                                      | কাজ          |
| ------ | ---------------------------------------- | ------------ |
| GET    | `/todos/api/todos/`                      | সব Todo দেখা |
| POST   | `/todos/api/todos/`                      | নতুন Todo    |
| GET    | `/todos/api/todos/{id}/`                 | একটি Todo    |
| PUT    | `/todos/api/todos/{id}/`                 | Todo আপডেট   |
| PATCH  | `/todos/api/todos/{id}/`                 | আংশিক আপডেট  |
| DELETE | `/todos/api/todos/{id}/`                 | Todo মুছা    |
| POST   | `/todos/api/todos/{id}/toggle_complete/` | টগল          |

---

## 💡 টিপস

1. **Browsable API ব্যবহার করুন** - curl এর চেয়ে সহজ
2. **কোড দেখুন** - সব ফাইলে বাংলা কমেন্ট আছে
3. **README_BANGLA.md পড়ুন** - বিস্তারিত জানতে

---

## 📁 গুরুত্বপূর্ণ ফাইল

```
todos/
├── models.py          → ডাটাবেস স্ট্রাকচার (বাংলা কমেন্ট সহ)
├── views.py           → লজিক (বাংলা কমেন্ট সহ)
├── serializers.py     → JSON কনভার্টার
├── urls.py            → URL রাউটিং
└── admin.py           → এডমিন সেটআপ
```

সব ফাইলে **বাংলা কমেন্ট** আছে বিগিনারদের জন্য!

---

## ✨ শুরু করুন!

এখনই শুরু করুন: **http://127.0.0.1:8000/**

**শুভ কোডিং! 🎉**
