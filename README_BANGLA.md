# Django Todo App - বাংলা গাইড

একটি সহজ **Todo অ্যাপ্লিকেশন** Django দিয়ে তৈরি যেখানে CRUD operations এবং REST API আছে।

## 🌟 কি কি আছে এই প্রজেক্টে

- ✅ **সম্পূর্ণ CRUD Operations** (Create, Read, Update, Delete)
- 🔌 **REST API** Django REST Framework দিয়ে
- 📱 **রেসপন্সিভ ডিজাইন** Bootstrap 5 দিয়ে
- ✓ **এক ক্লিকে টগল** - Todo সম্পন্ন/অসম্পন্ন করা
- 🎨 **সাদামাটা ডিজাইন** - বুঝতে সহজ
- 🔐 **এডমিন প্যানেল** - ম্যানেজমেন্টের জন্য

## 📁 প্রজেক্ট স্ট্রাকচার

```
Django CRUD Project/
│
├── core/                    # মূল প্রজেক্ট সেটিংস
│   ├── settings.py         # Django কনফিগারেশন
│   └── urls.py             # মূল URL রাউটিং
│
├── todos/                   # Todo অ্যাপ
│   ├── models.py           # Todo মডেল (ডাটাবেস টেবিল)
│   ├── views.py            # ভিউ ফাংশন (লজিক)
│   ├── serializers.py      # API সিরিয়ালাইজার
│   ├── urls.py             # URL প্যাটার্ন
│   └── admin.py            # এডমিন কনফিগ
│
├── templates/todos/         # HTML টেমপ্লেট
│   ├── base.html
│   ├── todo_list.html
│   ├── todo_form.html
│   └── todo_confirm_delete.html
│
└── manage.py               # Django কমান্ড চালানোর জন্য
```

## 🚀 কিভাবে চালাবেন

### ১. ডিপেন্ডেন্সি ইনস্টল করুন

```bash
pip install -r requirements.txt
```

### ২. ডাটাবেস মাইগ্রেশন করুন

```bash
python manage.py migrate
```

### ৩. এডমিন ইউজার তৈরি করুন (ঐচ্ছিক)

```bash
python manage.py createsuperuser
```

### ৪. সার্ভার চালু করুন

```bash
python manage.py runserver
```

### ৫. ব্রাউজারে খুলুন

- **ওয়েব অ্যাপ**: http://127.0.0.1:8000/
- **REST API**: http://127.0.0.1:8000/todos/api/todos/
- **এডমিন প্যানেল**: http://127.0.0.1:8000/admin/

## 📖 কিভাবে ব্যবহার করবেন

### ওয়েব ইন্টারফেস

1. **Todo দেখা**: http://127.0.0.1:8000/ এ যান
2. **Todo তৈরি**: "+ Add Todo" বাটনে ক্লিক করুন
3. **Todo আপডেট**: ✎ (এডিট) বাটনে ক্লিক করুন
4. **Todo মুছা**: 🗑 (ডিলিট) বাটনে ক্লিক করুন
5. **সম্পন্ন চিহ্নিত করা**: ✓ বাটনে ক্লিক করুন

### REST API ব্যবহার

#### সব Todo দেখা

```http
GET /todos/api/todos/
```

#### নতুন Todo তৈরি

```http
POST /todos/api/todos/
Content-Type: application/json

{
    "title": "Django শিখুন",
    "description": "Django টিউটোরিয়াল সম্পন্ন করুন",
    "completed": false
}
```

#### একটি Todo দেখা

```http
GET /todos/api/todos/{id}/
```

#### Todo আপডেট

```http
PUT /todos/api/todos/{id}/
Content-Type: application/json

{
    "title": "Django REST Framework শিখুন",
    "description": "DRF টিউটোরিয়াল সম্পন্ন করুন",
    "completed": true
}
```

#### আংশিক আপডেট

```http
PATCH /todos/api/todos/{id}/
Content-Type: application/json

{
    "completed": true
}
```

#### Todo মুছা

```http
DELETE /todos/api/todos/{id}/
```

#### টগল করা (কাস্টম এক্শন)

```http
POST /todos/api/todos/{id}/toggle_complete/
```

## 🎯 মডেল স্ট্রাকচার

```python
class Todo(models.Model):
    title = models.CharField(max_length=200)          # টাইটেল
    description = models.TextField(blank=True)        # বিস্তারিত
    completed = models.BooleanField(default=False)    # সম্পন্ন হয়েছে কিনা
    created_at = models.DateTimeField(auto_now_add=True)  # তৈরির তারিখ
    updated_at = models.DateTimeField(auto_now=True)      # আপডেটের তারিখ
```

## 🔧 PowerShell দিয়ে API টেস্ট

### Todo তৈরি:

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/" -Method Post -Headers @{"Content-Type"="application/json"} -Body '{"title":"আমার প্রথম Todo","description":"পরীক্ষামূলক"}'
```

### সব Todo দেখা:

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/"
```

### Todo আপডেট:

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/1/" -Method Patch -Headers @{"Content-Type"="application/json"} -Body '{"completed":true}'
```

### Todo মুছা:

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8000/todos/api/todos/1/" -Method Delete
```

## 🛠️ বেস্ট প্র্যাকটিস যা ফলো করা হয়েছে

### ১. **REST API ডিজাইন**

- ✅ RESTful URL প্যাটার্ন
- ✅ সঠিক HTTP মেথড (GET, POST, PUT, PATCH, DELETE)
- ✅ JSON রেসপন্স
- ✅ পেজিনেশন (১০ আইটেম/পেজ)

### ২. **Django বেস্ট প্র্যাকটিস**

- ✅ ক্লাস-বেসড ভিউ (ViewSet)
- ✅ মডেল Meta অপশন
- ✅ URL নেমস্পেসিং
- ✅ পরিচ্ছন্ন কোড স্ট্রাকচার

### ৩. **সিকিউরিটি**

- ✅ CSRF প্রোটেকশন
- ✅ CORS হেডারস
- ✅ SQL ইনজেকশন প্রিভেনশন
- ✅ XSS প্রোটেকশন

## 📦 ডিপেন্ডেন্সি

- **Django 5.2.7** - ওয়েব ফ্রেমওয়ার্ক
- **djangorestframework** - REST API টুলকিট
- **django-cors-headers** - CORS হ্যান্ডলিং
- **python-decouple** - এনভায়রনমেন্ট ভেরিয়েবল
- **Bootstrap 5.3** - UI ফ্রেমওয়ার্ক (CDN)

## 💡 কোড বুঝতে সাহায্য

### models.py

- এখানে ডাটাবেস টেবিলের স্ট্রাকচার ডিফাইন করা হয়
- `Todo` ক্লাস = ডাটাবেসে একটি টেবিল
- প্রতিটি ফিল্ড = টেবিলের কলাম

### views.py

- এখানে অ্যাপ্লিকেশনের লজিক লেখা হয়
- `TodoViewSet` = API এর জন্য (automatic CRUD)
- অন্য ফাংশন = ওয়েব পেজের জন্য

### serializers.py

- Django Model কে JSON এ কনভার্ট করে
- API এর জন্য দরকারি

### urls.py

- URL প্যাটার্ন ডিফাইন করা হয়
- কোন URL এ কোন ভিউ চলবে তা ঠিক করে

### admin.py

- এডমিন প্যানেল কাস্টমাইজ করা হয়
- লিস্ট, ফিল্টার, সার্চ সেটআপ

## 🎨 ফিচার সংক্ষেপ

**ওয়েব ইন্টারফেস:**

- পরিচ্ছন্ন ডিজাইন
- রেসপন্সিভ লেআউট
- দ্রুত টগল বাটন
- ইনলাইন এডিট/ডিলিট

**REST API:**

- সম্পূর্ণ CRUD অপারেশন
- ব্রাউজেবল API (DRF)
- পেজিনেশন
- কাস্টম এক্শন (toggle_complete)

## 📚 ফাইল বর্ণনা

| ফাইল             | কাজ                |
| ---------------- | ------------------ |
| `models.py`      | ডাটাবেস স্ট্রাকচার |
| `views.py`       | বিজনেস লজিক        |
| `serializers.py` | JSON কনভার্শন      |
| `urls.py`        | URL রাউটিং         |
| `admin.py`       | এডমিন কনফিগ        |
| `templates/`     | HTML ফাইল          |

## 🚀 পরবর্তী ধাপ

এই অ্যাপে আরো যোগ করতে পারেন:

- ইউজার লগইন/রেজিস্ট্রেশন
- ডিউ ডেট (শেষ তারিখ)
- প্রায়োরিটি লেভেল
- ক্যাটেগরি/ট্যাগ
- সার্চ ও ফিল্টার
- বাল্ক অপারেশন

## 📝 কমন কমান্ড

```bash
# সার্ভার চালু
python manage.py runserver

# এডমিন তৈরি
python manage.py createsuperuser

# মাইগ্রেশন তৈরি
python manage.py makemigrations

# মাইগ্রেশন চালু
python manage.py migrate

# ভিন্ন পোর্টে চালু
python manage.py runserver 8001
```

## ✨ সারসংক্ষেপ

এই প্রজেক্টে আছে:

- ✅ সহজ Todo অ্যাপ (১টি মডেল)
- ✅ সম্পূর্ণ CRUD (ওয়েব + API)
- ✅ REST API (Django REST Framework)
- ✅ বাংলা কমেন্ট (বিগিনারদের জন্য)
- ✅ বেস্ট প্র্যাকটিস ফলো করা

**বিগিনারদের জন্য পারফেক্ট! CRUD এবং REST API শেখার জন্য।** 🎉

---

## 🆘 সমস্যা সমাধান

**পোর্ট ব্যস্ত থাকলে:**

```bash
python manage.py runserver 8001
```

**ডাটাবেস এরর হলে:**

```bash
# db.sqlite3 ফাইল মুছে দিন
python manage.py migrate
```

**সার্ভার বন্ধ করতে:**
Terminal এ `Ctrl+C` চাপুন

---

**শুভ কোডিং! 🚀**

_এই প্রজেক্ট শিক্ষার উদ্দেশ্যে তৈরি। ফ্রি ব্যবহার করতে পারবেন।_
